import { AnonSigninDirective } from './anon-signin.directive';

describe('AnonSigninDirective', () => {
  it('should create an instance', () => {
    const directive = new AnonSigninDirective();
    expect(directive).toBeTruthy();
  });
});
